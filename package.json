{
  "name": "mediscan-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:all": "concurrently \"npm run dev --prefix mediscan-app\" \"npm run start --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
